//constant pt stocare temperaturii in kelvin
const kelvin = 293;
//declaring var celsius;
let celsius;
//doing the calculation to obtail the temp in cellsius kelvin - celsius
celsius = kelvin -273;
//doing the calc to obtain fahrenheit
let fahrenheit;
fahrenheit = celsius * (9/5) + 32;
//converting to roudn number;
fahrenheit = Math.floor(fahrenheit);
console.log(`The temperature is ${fahrenheit} degrees Fahrenheit.`);
let newton;
newton= celsius * (33/100);
newton = Math.floor(newton);
console.log(`The temperature is ${fahrenheit} degrees Fahrenheit.`);